FROM ghcr.io/berriai/litellm:main-latest

# Headroom guardrail plugin is built-in, no extra install needed
COPY config/litellm.yaml /app/config.yaml

ENV LITELLM_CONFIG=/app/config.yaml
ENV LITELLM_PORT=4000

EXPOSE 4000

ENTRYPOINT ["litellm", "--config", "/app/config.yaml", "--port", "4000"]
